---
name: Vitestテスト実行結果
overview: ローカルで `vitest run` を実行し、全テストが成功したことを記録する。ホーム画面仕様ケース1・4〜6と集計ユーティリティの対応、モック方針、未カバー範囲を整理する。
isProject: true
---

# Vitest テスト実行結果（プラン形式サマリ）

## 実行結果（実測）

| 項目 | 値 |
|------|-----|
| コマンド | `npx vitest run` |
| Vitest | 4.1.1 |
| テストファイル | **2** ファイルすべて成功 |
| テストケース数 | **11** 件すべて成功 |
| 所要時間（参考） | 約 4.9s（環境により変動） |
| 終了コード | **0**（成功） |

## テストファイルと内容

### [`src/utils/financeCalculations.test.ts`](src/utils/financeCalculations.test.ts)（4 件）

| 対象 | 検証内容 |
|------|----------|
| `financeCalculations` | 複数取引から収入・支出・残高を正しく集計する |
| `financeCalculations` | 空配列ならすべて 0 |
| `calclateDailyBalances` | 日付ごとに収入・支出・残高を集計（同一日の合算含む） |
| `calclateDailyBalances` | 空配列なら空オブジェクト |

### [`src/pages/Home.test.tsx`](src/pages/Home.test.tsx)（7 件）

Firestore・Firebase 初期化・カレンダーをモックしたうえで RTL で操作・表示を検証している。

| 仕様上の位置づけ | テスト名（要約） | 主なアサート |
|------------------|------------------|--------------|
| **ケース1** 初期表示 | 空データのとき月次サマリが ¥0 | `getDocs` 呼び出し、収入／支出／残高ラベル、`¥0` が複数 |
| **ケース4** 新規追加 | 内訳を追加でダイアログ | 「入力」表示 |
| **ケース4** | 支出時カテゴリ 6 件 | カテゴリ combobox の option が 6 |
| **ケース4** | 収入時カテゴリ 3 件 | 収入切替後 option が 3 |
| **ケース4** | 保存で `addDoc` | `type: "expense"`, `amount: 1000` を含む 1 回呼び出し |
| **ケース5** 削除 | 削除で一覧から消える | `deleteDoc`、文言「コンビニ」が DOM から消える |
| **ケース6** 更新 | 金額変更で `updateDoc` | `amount: 250000` を含む 1 回呼び出し |

## 補助実装（テスト基盤）

- [`src/test/mocks/firebaseFirestore.ts`](src/test/mocks/firebaseFirestore.ts) … `getDocs` 用スナップショット生成など
- [`src/test/renderWithApp.tsx`](src/test/renderWithApp.tsx) … テーマ・ルータ・コンテキスト付きレンダラ
- `Home.test.tsx` 内 … `firebase/firestore`・[`../firebase`](../firebase.ts)・[`Calendar`](../components/Calendar.tsx) を `vi.mock`（カレンダーは `data-testid="calendar"` のスタブ）

## 仕様ケース 1〜6 との対応（現状）

| ケース | 単体テストでの扱い |
|--------|-------------------|
| 1 | **一部**。月次／日次の ¥0 表示とラベルは検証。本物の FullCalendar の「更新表示」はスタブのため **DOM レベルでは未検証**（コメントどおり E2E 想定） |
| 2・3 | **未実装**（カレンダー操作は E2E 推奨の方針） |
| 4〜6 | **実装済み**（上表のとおり） |

## 推奨フォローアップ（任意）

1. **CI 用**: [`package.json`](../package.json) の `"test": "vitest"` を `vitest run` に変更するか、`test:ci` スクリプトを追加してウォッチと分離する。
2. **E2E**: ケース 2・3 およびケース 1 の「カレンダーが更新表示」を Playwright 等で補完する。

## 再実行手順

プロジェクトルートで次を実行する。

```bash
npx vitest run
```

ウォッチモードは `npm test`（`vitest`）のまま。
